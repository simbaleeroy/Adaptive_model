 Method createOrUpdateMethod = null;
                                Class<?> currentClass = createVcDrgV2AttachmentWorkflow.getClass();

                                while (currentClass != null && createOrUpdateMethod == null) {
                                        try {
                                                createOrUpdateMethod = currentClass.getDeclaredMethod(
                                                                "createOrUpdateDrgV2Attachment",
                                                                Transaction.class,
                                                                VirtualCircuitDo.class,
                                                                VpnRegion.class);
                                                System.out.println("Found method in class: " + currentClass.getName());
                                        } catch (NoSuchMethodException e) {
                                                System.out.println(
                                                                "Method not found in class: " + currentClass.getName());
                                                currentClass = currentClass.getSuperclass();
                                        }
                                }

                                if (createOrUpdateMethod == null) {
                                        throw new RuntimeException(
                                                        "Method createOrUpdateDrgV2Attachment not found in class hierarchy");
                                }
