try (Transaction t = virtualCircuitBl.beginTransaction("update-vc")) {
                        virtualCircuitDo = virtualCircuitBl.update(t,
                                        virtualCircuitBl.getOrThrow(t, virtualCircuitDo.getId()).toBuilder()
                                                        .gatewayId(TEST_DRG_ID)
                                                        .build());
                        t.commit();
                }
