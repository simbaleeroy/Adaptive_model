// Additional verification: Ensure the change was persisted to the database
    try (Transaction t = crossConnectGroupBl.beginTransaction("verify-persistence")) {
        CrossConnectGroupDo retrievedCcg = crossConnectGroupBl.getOrThrow(t, ccgID);
        assertEquals(tenantID, retrievedCcg.getTenantId());
    }
