 // Debug: Check what's in the stream
        byte[] streamBytes = baos.toByteArray();
        String streamContent = new String(streamBytes);
        System.out.println("DEBUG TEST: Raw stream content length: " + streamBytes.length);
        System.out.println("DEBUG TEST: Raw stream content: " + streamContent);
