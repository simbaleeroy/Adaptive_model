// Debug: Check if drgV2Helper is a mock
                                System.out.println("drgV2Helper class: " + drgV2Helper.getClass().getName());
                                System.out.println("Is drgV2Helper a mock? "
                                                + Mockito.mockingDetails(drgV2Helper).isMock());

                                createVcDrgV2AttachmentWorkflow.createDrgV2Attachment(null, t, null, virtualCircuitDo);
                                System.out.println("Method call completed without exception");
                                fail("Expected BmcException to be thrown");
                        } catch (BmcException e) {
                                // Expected exception - this is what we want
                                System.out.println("Caught expected BmcException: " + e.getMessage());
                                assertThat(e.getMessage()).contains("Internal Server Error");
                        } catch (Exception e) {
                                // Catch any other exception to see what's happening
                                System.out.println("Caught unexpected exception: " + e.getClass().getName() + " - "
                                                + e.getMessage());
                                e.printStackTrace();
                                fail("Unexpected exception: " + e.getMessage());
                        }
