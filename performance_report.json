// Disable the validation using reflection
    Field validationField = VirtualCircuitResource.class.getDeclaredField("validateVcAndDrgTenanciesAreSame");
    validationField.setAccessible(true);
    
    // Or mock the entire VirtualCircuitResource instance
    VirtualCircuitResource spyResource = spy(virtualCircuitResource);
    doNothing().when(spyResource).validateVcAndDrgTenanciesAreSame(anyString(), anyString());
    
    setupVcDependencies(true);
